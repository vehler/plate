# Database Configuration
# Development: Use mongodb://mongo/plate when running in Docker (docker-compose)
# Development: Use mongodb://localhost/plate when running locally
# Production: Use your production MongoDB URI
DATABASE_URI=mongodb://mongo/plate

# Payload CMS Configuration
PAYLOAD_SECRET=your-payload-secret-here
PAYLOAD_PUBLIC_SERVER_URL=http://localhost:3000
PAYLOAD_CONFIG_PATH=src/payload.config.ts

# Next.js Configuration
NODE_ENV=development
NEXT_PUBLIC_SERVER_URL=http://localhost:3000

# MongoDB Configuration (for Docker Compose)
MONGO_INITDB_DATABASE=plate
MONGO_ROOT_USER=admin
MONGO_ROOT_PASSWORD=secure_password_change_me

# Security and Performance
SESSION_SECRET=your-session-secret-here
CORSORIGINS=http://localhost:3000

# Docker Configuration
# Uncomment for production docker-compose.prod.yml
# PORT=3000
# HOSTNAME=0.0.0.0
